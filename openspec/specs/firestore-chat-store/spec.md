# firestore-chat-store Specification

## Purpose

ユーザー別の会話、run、順序付きイベントを永続化し、長時間実行中の応答をリアルタイムに購読するとともに過去セッションを再訪できる契約を提供する。

## Requirements

### Requirement: 外部Executionの終端状態をRunへ原子的に反映する
Firestore実装は、Run IDで所有Runを特定した制御側から外部Executionの終端状態を受け取ったとき、Runの終端状態、終了時刻、安全なエラーコード、およびセッションのactive run参照を一貫して更新できなければならない（SHALL）。既に終端状態のRunを再照会で復活させてはならず、同じ終端反映を複数回適用しても結果を重複させてはならない（MUST NOT）。

#### Scenario: Cloud Runの失敗をRunへ反映する
- **WHEN** 制御側がRun IDに対応するExecutionの失敗と安全なエラーコードを反映する
- **THEN** FirestoreはRunをfailed、終了時刻、エラーコードへ更新し、セッションのactive run参照を解除して後続Runを開始できるようにする

#### Scenario: Cloud RunのキャンセルをRunへ反映する
- **WHEN** 制御側がExecutionのキャンセル完了を反映する
- **THEN** FirestoreはRunをcancelledとして保存し、セッションの最新Run状態を更新する

#### Scenario: 終端Runを再照会する
- **WHEN** failed、cancelled、completedのいずれかに確定したRunへ同じまたは異なる再照会結果を反映しようとする
- **THEN** Firestoreは既存の終端状態を実行中へ戻さず、所有権と状態遷移の整合性を維持する

#### Scenario: 複数のreconcilerが同時に更新する
- **WHEN** 複数の制御プロセスが同じRun IDの終端反映を同時に要求する
- **THEN** Firestoreはreconciliation leaseまたは同等の排他境界で1つの更新だけを有効にし、active run解除と保持期限更新を壊さない

### Requirement: ユーザーとセッションを分離する
システムはすべてのセッション、run、イベントをユーザー ID の名前空間へ格納し、要求ユーザーが所有しないデータを取得、購読、更新してはならない（MUST NOT）。外部識別子を未検証のドキュメントパスとして使用してはならない（MUST NOT）。

#### Scenario: 他ユーザーのセッションを指定する
- **WHEN** 利用者が別ユーザーに属するセッション ID を指定する
- **THEN** システムは存在の詳細を漏らさずアクセスを拒否する

### Requirement: セッションを作成して一覧取得する
システムはユーザー別にセッションを作成し、更新日時の降順で安定したページングを行う一覧を返さなければならない（SHALL）。各項目にはセッション ID、表示用タイトル、作成日時、更新日時、最新 run の状態を含めなければならない（SHALL）。

#### Scenario: 過去セッションを一覧表示する
- **WHEN** 利用者が自分のセッション一覧を要求する
- **THEN** システムは当該ユーザーのセッションだけを更新日時の新しい順で返す

#### Scenario: 次ページを取得する
- **WHEN** 利用者が前ページで返されたカーソルを指定する
- **THEN** システムは重複や欠落を生じない安定した並びで次ページと次カーソルを返す

### Requirement: run要求を原子的に登録する
システムはユーザーメッセージ、run レコード、セッションの active run 参照を原子的に登録し、1 セッションに複数の active run を許可してはならない（MUST NOT）。同じ冪等キーの再送では既存 run を返さなければならない（SHALL）。

#### Scenario: 新しいrunを登録する
- **WHEN** active run のないセッションへ有効なメッセージと冪等キーを送る
- **THEN** システムはメッセージと新しい run を登録し、その run をセッションの active run とする

#### Scenario: active runが存在する
- **WHEN** active run のあるセッションへ別の run を登録しようとする
- **THEN** システムは既存 run の識別子と状態を返して新規登録を拒否する

### Requirement: runイベントを順序付きで追記する
システムは各イベントへ一意な event ID、run 内で単調増加する sequence、種別、発生時刻、payload を付けて追記しなければならない（SHALL）。同じ event ID の再送は重複イベントを作成してはならない（MUST NOT）。

#### Scenario: 複数イベントを保存する
- **WHEN** ジョブが応答、ツール利用、進捗、終端イベントを生成する
- **THEN** システムは run 内の確定した sequence 順でイベントを取得できるよう保存する

#### Scenario: イベント追記を再試行する
- **WHEN** 一時障害後に同じ event ID の追記が再送される
- **THEN** システムは既存イベントを返し、同じ論理イベントを二重表示させない

### Requirement: イベントをリアルタイム購読する
システムはセッションまたは run のイベントをリアルタイム購読でき、購読開始時のカーソルより後のイベントを順序付きで通知しなければならない（SHALL）。再配信が発生してもクライアントが event ID と sequence で重複を除ける情報を返さなければならない（SHALL）。

#### Scenario: 実行中の応答を受信する
- **WHEN** UI が run を購読中にジョブが新しいエージェントイベントを保存する
- **THEN** UI は定期的な全件ポーリングを行わず新しいイベントを増分受信できる

#### Scenario: 切断後に再購読する
- **WHEN** UI が最後に確認したカーソルを指定して購読を再開する
- **THEN** システムはそのカーソルより後の永続化済みイベントと以後の新規イベントを通知する

### Requirement: runの終端状態を整合して保存する
システムは completed、failed、cancelled、timed_out の終端状態、最終結果または安全なエラー、実行参照、snapshot 参照を run レコードへ保存しなければならない（SHALL）。completed は有効な snapshot commit と最終応答が保存された後にだけ設定しなければならない（SHALL）。

#### Scenario: 正常完了を保存する
- **WHEN** エージェント実行と snapshot 保存が成功する
- **THEN** システムは snapshot 参照と最終応答を保存した後に run を completed とし、セッションの active run を解除する

#### Scenario: 後続runが失敗する
- **WHEN** 直近の成功 run を持つセッションで後続 run が failed、cancelled、timed_out、または dispatch_failed になる
- **THEN** システムは active run を解除して当該 run の終端状態を保存するが、セッションの直近成功済み Claude セッション ID と snapshot 参照を消去または失敗 run の空値で上書きしてはならない

### Requirement: ストア実装を交換できる
セッション、run、イベントの操作は Firestore 固有型を公開契約へ漏らさず、同じ整合性、ページング、購読契約を満たす別ストア実装へ交換できなければならない（SHALL）。

#### Scenario: テスト用ストアへ差し替える
- **WHEN** 利用者がインメモリ実装を構成する
- **THEN** 制御クライアントとジョブライフサイクルは Firestore 接続なしで同じ公開操作を実行できる

### Requirement: 初回メッセージでセッションを原子的に開始する
システムは新規会話の有効な最初のメッセージと冪等キーを受け取ったときにだけセッション ID と workspace ID を割り当て、表示用セッション名、ユーザーメッセージ、最初の run、および active run 参照を原子的に保存しなければならない（SHALL）。表示用セッション名は最初のメッセージから空でない人間可読な値として決定し、後続 run によって暗黙に変更してはならない（MUST NOT）。

#### Scenario: 新規会話へ最初のメッセージを送る
- **WHEN** 利用者が未開始の新規会話へ有効なメッセージと冪等キーを送る
- **THEN** システムは名前付きセッションと最初の run を一度だけ作成し、そのセッション ID と run を返す

#### Scenario: 空の新規会話を表示する
- **WHEN** 利用者が新規会話を開いたままメッセージを送らず離れる
- **THEN** システムはセッション、run、イベント、workspace の識別子または永続レコードを作成しない

#### Scenario: 初回開始要求を再送する
- **WHEN** 同じ利用者が同じ冪等キーで初回開始要求を再送する
- **THEN** システムは既存のセッションと run を返し、空または重複したセッションを追加しない

### Requirement: セッションの全会話履歴を再取得する
システムは所有者がセッションを再訪するために、そのセッションに属する run と表示可能な会話イベントを確定順序で取得できる契約を提供しなければならない（SHALL）。複数 run のイベントは run の開始順、各 run 内の sequence と event ID の順で安定して返さなければならない（SHALL）。

#### Scenario: 複数runを持つセッションを再訪する
- **WHEN** 利用者が複数の完了済み run を持つ自分のセッション履歴を要求する
- **THEN** システムは各 run のユーザーメッセージとエージェント応答を会話順に復元できる形で返す

#### Scenario: 実行中のセッションを再訪する
- **WHEN** 利用者が active run を持つ自分のセッション履歴を要求する
- **THEN** システムは永続化済み履歴、active run とその現在状態、および増分購読を再開するための識別情報を返す

### Requirement: 期限切れのFirestoreチャットデータを扱う
Firestore 実装はセッション、run、イベントの各ドキュメントへ基準時刻からリリース設定で指定された共通保持期間後の有効期限を保存し、期限に達したドキュメントを読み取り結果と一覧から除外しなければならない（SHALL）。共通保持期間の既定値は 30 日としなければならない（SHALL）。物理削除が遅延しても期限切れデータを利用者へ再表示してはならず（MUST NOT）、セッション親ドキュメントの削除後に run またはイベントが無期限に残留しないよう各階層を自動削除の対象にしなければならない（SHALL）。

#### Scenario: 既定の有効期限を保存する
- **WHEN** リリース設定で保持期間を省略してセッション、run、イベントを新規作成または保持期限を更新する
- **THEN** 各ドキュメントには対応する基準時刻から 30 日後の有効期限が保存される

#### Scenario: 設定した有効期限を保存する
- **WHEN** リリース設定で有効な共通保持期間を指定してセッション、run、イベントを新規作成または保持期限を更新する
- **THEN** 各ドキュメントには対応する基準時刻から指定日数後の有効期限が保存される

#### Scenario: 期限到達から物理削除まで遅延する
- **WHEN** ドキュメントの有効期限を過ぎたが自動削除処理がまだ完了していない
- **THEN** セッション一覧と履歴取得はその期限切れドキュメントを返さない

#### Scenario: セッション階層を自動削除する
- **WHEN** セッション、run、イベントの各ドキュメントがそれぞれの有効期限へ到達する
- **THEN** Firestore は各ドキュメントを手動操作なしで削除対象とし、子階層だけを無期限に保持しない

### Requirement: runの質問要求と回答を原子的に永続化する
システムは run ごとの質問要求 ID、質問内容、選択肢、複数選択可否、状態、回答、および更新時刻を永続化し、質問要求を `pending` から `answered` へ原子的に一度だけ遷移させなければならない（SHALL）。同じ冪等性キーによる回答再送は既存結果を返し、異なる回答による上書きや終端済み run への新規回答を許可してはならない（MUST NOT）。

#### Scenario: pending質問へ初めて回答する
- **WHEN** run の所有者が pending 質問要求 ID と有効な回答を送信する
- **THEN** ストアは回答を保存して質問を answered にし、待機中のジョブが取得できるよう通知する

#### Scenario: 同じ回答を再送する
- **WHEN** 同じ質問要求と冪等性キーを持つ回答が再送される
- **THEN** ストアは同じ answered 状態を返し、回答レコードまたは通知を重複作成しない

#### Scenario: 競合する回答が到着する
- **WHEN** 同じ pending 質問へ異なる回答が同時に送信される
- **THEN** ストアは最初に確定した回答だけを受理し、後続回答へ競合を返して確定値を上書きしない

#### Scenario: 終端済みrunへ回答する
- **WHEN** completed、failed、cancelled、timed_out、または dispatch_failed の run に回答が送信される
- **THEN** ストアは回答を保存せず、run が回答受付を終了していることを返す

### Requirement: 質問回答を所有者境界と保持期間に従って扱う
システムは質問要求の取得、購読、回答を run 所有者の名前空間に限定し、他の利用者へ質問内容または回答の存在を漏らしてはならない（MUST NOT）。質問要求と回答は親 run と同じ保持期限および期限切れ除外規則に従わなければならない（SHALL）。

#### Scenario: 他ユーザーの質問へ回答する
- **WHEN** 利用者が別ユーザーに属する run の質問要求 ID を指定する
- **THEN** システムは質問の存在や状態の詳細を漏らさずアクセスを拒否する

#### Scenario: 質問データの保持期限が切れる
- **WHEN** 親 run と質問要求の保持期限に到達する
- **THEN** システムは質問と回答を取得・購読結果から除外し、無期限に残留させない

### Requirement: ジョブが質問回答を切断耐性付きで待機できる
ストアは pending 質問の現在状態を取得し、回答確定を購読し、購読切断後に同じ質問要求 ID から再開できる契約を提供しなければならない（SHALL）。通知の再配信が発生しても、質問要求 ID と状態により同じ回答を一度だけ適用できる情報を返さなければならない（SHALL）。

#### Scenario: 回答通知の前に購読が切断する
- **WHEN** ジョブの回答購読が一時的に切断し、その間に回答が保存される
- **THEN** ジョブは再取得または再購読により確定済み回答を取得でき、質問を再発行しない
