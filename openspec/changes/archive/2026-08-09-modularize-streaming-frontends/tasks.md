## 1. 共通チャットモジュール

- [x] 1.1 フロントエンド非依存パッケージを追加し、release config から `ControlClient` を生成する処理とユーザー identity 境界を移す
- [x] 1.2 新規・既存セッションで冪等に run を開始し、session/run 識別子を返す会話サービスを実装する
- [x] 1.3 保存済みイベントからカーソル付き購読へ接続し、イベント ID の重複排除、終端判定、一時エラー後の再接続を行うストリームを実装する
- [x] 1.4 エージェント本文、ツール、進捗、終端、未知イベントを扱えるフロントエンド非依存のイベント表現と履歴正規化を実装する
- [x] 1.5 新規・既存セッション、冪等な再送、イベント順序、境界重複、再接続、終端、未知イベントの単体テストを追加する

## 2. CLIによるストリーミング検証

- [x] 2.1 release config、user ID、prompt、任意の session ID と idempotency key を受ける CLI エントリーポイントを追加する
- [x] 2.2 開始結果とイベントを一レコードずつ JSON Lines で即時 flush し、診断と失敗終了コードを分離する
- [x] 2.3 CLI の引数検証、逐次出力、既存セッション継続、成功・失敗・キャンセル時の終了コードをテストする
- [x] 2.4 モックまたはローカルテスト用ポートで CLI を実行し、応答が終端前から逐次取得できることを確認する

## 3. サンプル構成とStreamlit移行

- [x] 3.1 `example/agent.py` を `example/agent` パッケージへ移し、Dockerfile とジョブエントリーポイントの参照を更新する
- [x] 3.2 `sample_frontend` を `example/streamlit_frontend` パッケージへ移し、セッション/run/履歴/購読処理を共通チャットモジュール利用へ置き換える
- [x] 3.3 Streamlit のセッション一覧、再訪、増分表示、キャンセル、異常終了表示に関する既存テストを新パスと共通境界へ更新する
- [x] 3.4 package discovery とリポジトリ内 import・起動パスを新構成へ更新し、旧 `example/agent.py` と `sample_frontend` を削除する

## 4. Slackスレッド対応の永続化

- [x] 4.1 Slack team/channel/thread とアプリケーション user/session を対応付けるストアポートを定義する
- [x] 4.2 release config の名前付き Firestore database を使う対応ストアを、条件付き作成と再取得を含めて実装する
- [x] 4.3 対応の作成・再取得、プロセス再起動相当の再生成、競合、利用者分離をエミュレーターまたはテストダブルで検証する

## 5. Slack Botフロントエンド

- [x] 5.1 Slack SDK を専用 dependency group に追加し、Socket Mode の認証情報と release config を環境変数・引数から検証して読み込む
- [x] 5.2 対象メッセージを即時 acknowledge し、Bot 自身のイベントを除外して、Slack event ID 由来の冪等性キーでバックグラウンド run を開始するハンドラーを実装する
- [x] 5.3 新規スレッドのセッション対応作成と、既存スレッドでのセッション継続を共通チャットモジュールへ接続する
- [x] 5.4 応答イベントを集約して同一スレッドのメッセージを間引き更新し、ツール進捗、終端、失敗、および rate limit の再試行を表示する
- [x] 5.5 重複配送、自己イベント除外、スレッド継続、増分更新の間引き、rate limit、秘密を含まないエラー表示を Slack API テストダブルで検証する

## 6. ドキュメントと全体検証

- [x] 6.1 README に旧パスから新パスへの対応、依存 group の導入、CLI の新規・継続会話と JSON Lines ストリーム確認手順を記載する
- [x] 6.2 README に Slack App の Socket Mode、必要権限、イベント購読、環境変数、Bot 起動、スレッド継続の手順を秘密値の保存なしで記載する
- [x] 6.3 ジョブ配備、Streamlit、セッション再訪、キャンセル、障害確認の既存手順とサンプル設定を新パスに合わせて更新する
- [x] 6.4 全テスト、ruff、mypy、パッケージ build を実行し、CLI と各フロントエンドの import・起動 smoke test を完了する
